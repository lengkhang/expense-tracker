import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({
  inputButton: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 250,
    width: '80%',
    height: '80%'
  },
  inputButtonText: {
    fontSize: 40,
    fontWeight: 'bold'
  }
});